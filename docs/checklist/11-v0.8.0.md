# Checklist v0.8.0

## Obiettivo
Prima ondata adapter esterni ad alta utilità e basso rischio licenza.

## Checklist
- [ ] Implementare adapter `CommandPort` (CommandAPI e/o Cloud).
- [ ] Implementare adapter `ScoreboardPort` (FastBoard).
- [ ] Implementare adapter `HologramPort` (FancyHolograms) opzionale.
- [ ] Implementare adapter `NpcPort` (FancyNpcs) opzionale.
- [ ] Capability detection robusta (plugin presente/versione compatibile).
- [ ] Fallback no-op trasparente quando adapter non disponibile.
- [ ] Documentare setup server-side richiesto per ogni adapter.

## Test obbligatori
- [ ] Integration smoke tests con e senza ogni plugin adapter installato.
- [ ] Test graceful degradation in assenza dipendenze.
- [ ] Test startup ordering (es. hook post-load plugin esterni).

## Exit criteria
- [ ] Adapter wave 1 pronta per adozione in plugin reali.
