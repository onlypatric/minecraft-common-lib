# Release Notes `1.0.0-rc.1`

Data: `2026-03-04`
Branch: `release/1.0.0-rc.1`
Scope: candidate pre-GA con freeze API formale, cleanup legacy e hardening qualità.

## Highlights
- Freeze API `1.0.0` formalizzato su `api/*` e `api/port/*`.
- Rimozione API legacy deprecate pre-1.0 (`PluginLifecycle`, `Tasks`, `MiniMessageService`).
- Policy stabilità/versioning post-1.0 allineata (SemVer standard).
- Gate Javadoc impostato fail-fast (`-Werror`).
- Hardening runtime HUD: nessun scheduling in constructor, bootstrap sicuro in `onLoad`.

## Smoke Runbook (manuale su Paper reale)

### Prerequisiti
- Java 21+
- Server Paper `1.21.11`
- Plugin consumer demo buildato contro jar locale common-lib.

### Scenario A (adapter stack attivo)
- Validazione primaria eseguita via matrix/test harness:
  - `./gradlew --no-daemon adapterIntegrationTest -PrunAdapterIntegration=true`
  - `./gradlew --no-daemon externalMatrixTest -PrunExternalMatrix=true`
- Esito: `PASS`.

### Scenario B (degrade/fallback senza plugin esterni)
- Ambiente locale:
  - `bin/smoke-1.0.0-rc.1`
  - Paper `1.21.11-69`
  - solo `consumer-demo.jar` in `plugins/`
- Esito osservato:
  - plugin load/enable ok:
    - `[ConsumerDemoPlugin] Loading server plugin ConsumerDemoPlugin v1.0.0`
    - `[ConsumerDemoPlugin] Enabling ConsumerDemoPlugin v1.0.0`
  - server start ok:
    - `Done (...)! For help, type "help"`
  - shutdown ok:
    - `Stopping server`
    - `[ConsumerDemoPlugin] Disabling ConsumerDemoPlugin v1.0.0`
  - nessuna riga `ERROR|SEVERE|Exception` nel log finale.

## Gate eseguiti su RC candidate
- `./gradlew --no-daemon clean test javadoc build`
- `./gradlew --no-daemon check`
- `./gradlew --no-daemon adapterIntegrationTest -PrunAdapterIntegration=true`
- `./gradlew --no-daemon externalMatrixTest -PrunExternalMatrix=true`
- `./gradlew --no-daemon integrationTest -PrunIntegrationHarness=true`
- `./gradlew --no-daemon -p examples/consumer-demo clean test -PcommonLibJar=../../build/libs/minecraft-common-lib-1.0.0-rc.1.jar`

Esito complessivo: `PASS`.
