package dev.patric.commonlib.api.packet;

/**
 * Packet traffic direction.
 */
public enum PacketDirection {
    /** Inbound packet from player to server. */
    INBOUND,
    /** Outbound packet from server to player. */
    OUTBOUND
}
